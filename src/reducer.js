var initialState = {
    userid : 0,
    userName : "",
    userUserName : "",
    orderId : 0,
    products : [],
    total: 0
}

export default function reducer(state = initialState) {
    switch(action.payload) {
        default:
        return state
    }
}